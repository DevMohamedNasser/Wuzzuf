import * as z from "zod";

export const updateAccSchema = {
    body: {
        mobileNumber: 
    }
}
/** 
 * 1. Update user account. (2 Grades)
    - you can update ( mobileNumber, DOB ,firstName, lastName, Gender)
    - if user update the mobileNumber make sure that you encrypt the new value
 */