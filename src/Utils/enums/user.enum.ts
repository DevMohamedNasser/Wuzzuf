export enum RoleEnum {
  User = 0,
  Admin = 1,
}

export enum GenderEnum {
  Male = 0,
  Female = 1,
}

export enum UserOTPEnum {
  ConfirmEmail = "ConfirmEmail",
  ForgetPassword = "ForgetPassword",
}

export enum ProviderEnum {
  System = 0,
  Google = 1,
}
